package docker

const (
	// defaultProtocol is the default network protocol for ports.
	defaultProtocol = "tcp"

	// defaultHost is the default host for container endpoints.
	defaultHost = "localhost"
)
